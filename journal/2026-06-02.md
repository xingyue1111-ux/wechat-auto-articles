## 2026-06-02

### 已确认设计
- 公众号主要交付从四张纯图片长图改为可复制 HTML 正文。
- 正文采用 1400-1600 字完整文章，围绕一个核心观点连续叙事。
- 每篇固定生成 4 张 Seedream 配图，默认跟随 HTML 一起复制。
- 后台同时提供 4 张配图下载入口，便于公众号未稳定接管外部图片时手动替换。
- 现有四张 1080px 宽长图继续保留，用于朋友圈、存档和备用导出。
- 第一版不接公众号接口，不自动创建草稿。

### 已完成
- 新增规格文档：`docs/superpowers/specs/2026-06-02-wechat-html-publishing-design.md`。
- 新增实施计划：`docs/superpowers/plans/2026-06-02-wechat-html-publishing.md`。

### 待执行
- 按实施计划完成代码修改、自动化检查和本地浏览器验证。
- 实现完成后，需要在微信公众号后台发布一篇测试文章，通过正式文章链接确认正文和 4 张图片仍然可见。

### 实施完成内容
- 将公众号主要交付改为可复制 HTML 正文。
- 保留 4 张 Seedream 配图下载和 4 张备用长图。
- Seedream 已配置时，生成或下载连续失败会中止任务。
- 后台生成完成后进入 `/admin/article/YYYY-MM-DD` 发布工作台，归档页也指向同一发布稿入口。

### 实施检查结果
- `npm.cmd run test:run`：21 个测试文件、76 个测试通过。
- `npm.cmd run lint`：通过。
- `npm.cmd run build`：通过，构建路由包含 `/admin/article/[date]`。
- 本地开发服务检查：`/admin`、`/archive`、`/login` 正常返回；无本地 manifest 时 `/admin/article/2026-06-02` 和 `/article/2026-06-02` 正常返回 404。
- in-app browser 连接失败，原因为 Browser 插件在 Windows sandbox 初始化时报错；本次改用 HTTP 响应做路由级检查。
- 未在验收阶段触发真实生成，避免额外调用外部抓取、DeepSeek、Seedream 和 Blob 服务。

### 待人工验收
- 生成一篇正式测试稿后，进入后台发布工作台。
- 将 HTML 正文粘贴到公众号编辑器。
- 提交发布一篇测试文章。
- 使用正式文章链接确认文字和 4 张图片仍然可见。
- 若外部图片未被公众号稳定接管，使用后台下载入口手动替换图片后再次发布。
