## 2026-06-01

### 完成内容
- 五路公开信号源统一限制为滚动过去 24 小时，每路最多 20 条，总候选池最多约 100 条。
- 增加跨期链接优先排除和新鲜度排序，减少连续多次生成相似内容。
- DeepSeek 输出结构校验失败时自动重试一次；仍失败时明确降级，并把原因写入日志和 manifest。
- manifest 保存候选素材、来源统计、排除链接、最终引用链接和 DeepSeek 状态，方便后续排查。
- Seedream 改为宽幅配图请求，长图渲染不再裁切配图；归档页和生成台展示自动选择的封面。

### 关键决策
- 严格使用滚动过去 24 小时，不用更旧新闻补齐数量。
- 分类、筛选、选题由 DeepSeek 在一次固定结构生成中完成，避免增加额外调用和复杂状态。
- 封面默认选择第一张 Seedream 主题图；历史数据兼容回退到首张原始配图或首张长图。

### 检查结果
- `npm.cmd run test:run`
- `npm.cmd run lint`
- `npm.cmd run build`

### 后续调整
- Seedream 配图从 5 张减少为 4 张，与 4 张公众号长图一一对应。
- 移除封面长图中额外的装饰配图，减少生图成本并简化版式。
- 将“独立内容排版工作台”记录到需求池，后续再讨论输入形式、随机模板和模板复用。
