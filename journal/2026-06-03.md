## 2026-06-03

### DeepSeek JSON 兜底问题
- 现象：线上生成时 DeepSeek 首次结构校验失败，第二次返回内容不是合法 JSON，系统降级为规则化兜底内容。
- 判断：新公众号正文要求 1400-1600 字，再加 10 个 panel、配图提示词和来源链接，原 `max_tokens: 5000` 容易导致 JSON 被截断。
- 处理：将 DeepSeek `max_tokens` 提高到 8000，并在 API 返回 `finish_reason: "length"` 时给出明确的“输出被截断”错误。
- 检查：`npm.cmd run test:run`、`npm.cmd run lint`、`npm.cmd run build` 均通过。

### 后续观察
- 线上再次生成时，如果仍降级，需要看日志是否显示“输出被截断”还是结构校验失败。
- 如果仍是结构校验失败，下一步应细分校验原因，明确提示是标题、panel 顺序还是字数不合格。

### 公众号发布后图片消失
- 现象：复制 HTML 到公众号后，编辑器预览可以显示图片，但正式发布后图片消失。
- 判断：公众号预览阶段能加载外部图片链接，不代表发布后会稳定保留外链图片。
- 处理：复制用 HTML 不再输出外链 `<img>`，改为输出“请在此处手动上传配图 X”的占位提示。
- 工作台文案改为提醒用户粘贴正文后手动上传 4 张配图，右侧继续提供 Seedream 图片下载入口。
- 检查：`npm.cmd run test:run`、`npm.cmd run lint`、`npm.cmd run build` 均通过。

### 连续几天标题相同排查
- 现象：后台列表中 2026-05-31 至 2026-06-03 多篇文章都显示“企业 AI 落地信号图”，用户怀疑新闻没有更新或主题素材太少。
- 判断：归档详情和生成日志显示候选源在更新，06-02、06-03 的来源和正文主题不同；主要问题是 DeepSeek 校验失败后进入规则兜底，而兜底顶层标题固定，后台列表也没有显示生成模式。
- 处理：兜底文章标题改为使用当天主素材的中文标题；后台已保存简报列表增加“DeepSeek 正文 / 规则兜底 / 生成模式未知”标识。
- 检查：`npm.cmd run test:run`、`npm.cmd run lint`、`npm.cmd run build --cache C:\tmp\npm-cache` 均通过。

### 同日多次生成与配图一致性
- 现象：发布稿中的“查看备用长图”可能打开日期级文章，和当前发布稿不是同一次生成；同一天多次点击生成时，历史入口也容易表现为一天一篇。
- 判断：生成流程已经写入 `runs/run-.../manifest.json`，但列表、跳转和页面读取仍主要按日期读取 `articles/date/manifest.json`。
- 处理：manifest 增加 `revision`；后台列表展示同一天的每次 run；发布稿、备用长图、归档和生成完成跳转都携带 `?run=...`，确保正文和备用长图来自同一次生成。
- 现象：4 张 Seedream 配图构图接近，像同一组雷达/仪表盘。
- 判断：旧逻辑只抽取几个 panel 的泛化 imagePrompt，统一风格词过强、板块语义差异太弱。
- 处理：Seedream 提示词改为按公众号正文的 4 个插图位置生成：开场综述、第一论点、第二论点、第三论点；每张图都包含对应板块标题、正文语义和不同构图指令。
- 检查：`npm.cmd run test:run`、`npm.cmd run lint`、`npm.cmd run build --cache C:\tmp\npm-cache` 均通过。

### 通用占位标题复发
- 现象：最新生成的发布稿标题仍显示“企业 AI 落地信号图”。
- 判断：上次只修了规则兜底标题；这次是 DeepSeek 正常返回 JSON 时照抄了 prompt 示例里的通用标题，normalize 只检查中文，没有拦截占位标题。
- 处理：prompt 明确禁止“企业 AI 落地信号图 / 企业 AI 日报 / 本期总标题”等占位标题，并移除 JSON 示例中的错误标题。
- 处理：DeepSeek 正常输出后，如果顶层标题或封面标题是占位标题，会改用正文里第一个具体板块标题。
- 处理：读取已保存 manifest 时也做同样的内存修复，因此当前已保存的坏标题在页面读取时也会被纠正。
- 检查：`npm.cmd run test:run`、`npm.cmd run lint`、`npm.cmd run build --cache C:\tmp\npm-cache` 均通过。

### 项目规则更新
- 用户确认将“公众号文章标题不得使用通用占位标题，必须通过测试拦截”写入项目级 `AGENTS.md`。

### 公众号正文多套排版
- 现象：用户希望复制到公众号的正文排版不要每次重复。
- 处理：为公众号 HTML 增加 4 套内联排版：editorial-line、warm-card、teal-digest、amber-memo。
- 处理：按每次生成的 `revision` 稳定选择版式；同一篇文章重复打开保持一致，相邻 run 会切换排版。
- 约束：仍然不输出 `<style>` 和 `<img>`，保留手动上传图片占位，避免公众号发布后外链图片消失。
- 检查：`npm.cmd run test:run`、`npm.cmd run lint`、`npm.cmd run build --cache C:\tmp\npm-cache` 均通过。
